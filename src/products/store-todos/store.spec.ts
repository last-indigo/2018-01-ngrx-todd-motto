import * as fromStore from './';

describe('store', () => {
    it('exists', () => {
        expect(fromStore.Store).toEqual(jasmine.any(Function));
    });

    // it('creates a store', () => {
    //     expect({ ... new fromStore.Store() }).toEqual({
    //         value: {}
    //     });        
    // })
})